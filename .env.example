# ── Database (Cloud SQL Postgres or local) ──────────────────
DATABASE_URL="postgresql://user:pass@localhost:5432/a6ko?schema=public"

# ── Better Auth ─────────────────────────────────────────────
BETTER_AUTH_SECRET="generate-a-long-random-string"
BETTER_AUTH_URL="https://studio.a6ko.com"

# ── SSO providers ───────────────────────────────────────────
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
APPLE_CLIENT_ID=""          # requires Apple Developer Program ($99/yr)
APPLE_CLIENT_SECRET=""      # generated JWT, rotate per Apple rules
FACEBOOK_CLIENT_ID=""
FACEBOOK_CLIENT_SECRET=""
TIKTOK_CLIENT_KEY=""        # requires TikTok app review
TIKTOK_CLIENT_SECRET=""
# Instagram: intentionally not configured — no viable consumer login (see MIGRATION.md)

# ── AI providers (server-side only — never ship to browser) ─
GEMINI_API_KEY=""           # ROTATE the old leaked key before using
OPENAI_API_KEY=""           # for gpt-image-1

# ── Payments ────────────────────────────────────────────────
MONEROO_SECRET_KEY=""
MONEROO_WEBHOOK_SECRET=""

# ── Storage (generated images) ──────────────────────────────
GCS_BUCKET=""
